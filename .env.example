# Copy this file to ".env" and fill in your own values.
# (.env itself is gitignored so real credentials are never committed.)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=todo_db
