# Read automatically by the `flask` CLI (python-dotenv is installed).
# Points the CLI at run.py's `app` so `flask db migrate`, `flask db upgrade`,
# and `flask run` work without setting FLASK_APP by hand each time.
FLASK_APP=run
